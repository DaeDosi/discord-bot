# CHANGELOG

세션 단위로 주요 작업을 정리합니다. 커밋 메시지가 더 자세하니, 특정 항목의 정확한 diff가
필요하면 `git log --oneline`으로 관련 커밋 해시를 찾아 `git show <hash>`로 확인하세요.

## 2026-07-14 세션

**출발점**: nexbot.shop/nexadmin이 "집계 중..."에서 멈추는 문제 + 구글 애드센스가
"게시자 콘텐츠가 없는 화면에 광고" / "가치가 별로 없는 콘텐츠"로 "주의 필요" 판정을 준
문제를 같이 조사해달라는 요청에서 시작.

### 1. nexadmin "집계 중..." 무한 로딩 — 원인이 두 개 겹쳐 있었음

- **원인 A — 백엔드 성능** (`4a95c57`): `follow-stats`/`verifications`가 인증 유저마다
  개별 Discord 멤버 조회(`GET /guilds/{id}/members/{user_id}`)를 했는데, 인증자가 많은
  길드는 응답이 수십 초~수 분까지 걸렸음. `admin_router.py`에 `_guild_member_map()`을
  추가해 길드당 1회 멤버 목록 조회(최대 1000명 페이지네이션, 120초 캐시)로 교체 — N번
  요청을 길드 수만큼으로 줄임.
- **원인 A 보강** (`4a95c57`): 프론트 `adminFetch`에 타임아웃(20초)이 아예 없었고, Phase 2
  실패는 빈 `catch{}`로 조용히 삼켜졌음 → 타임아웃 추가 + 통계 카드 아래 에러 배너 +
  재시도 버튼 추가.
- **원인 B — 진짜 근본 원인** (`ea65b57`): 위 수정을 배포한 뒤에도 여전히 "집계 중..."이
  떠서 재조사. 로그를 보니 `/api/admin/guilds`, `/api/admin/chzzk`가 **401**로 실패하고
  있었는데, 프론트가 `e.message.includes("접근")`이라는 문자열 매칭으로만 `authed=false`를
  판정해서(403 "접근 권한이 없습니다"만 잡고 401 "인증이 필요합니다"는 못 잡음) `authed`가
  `null`인 채로 남아 로그인 안내 화면 대신 빈 대시보드가 그대로 렌더링됐음. `adminFetch`가
  상태 코드를 담은 `AdminApiError`를 던지도록 바꾸고, 401/403 모두 `authed=false`로,
  그 외(네트워크/타임아웃) 실패는 별도 에러 화면+재시도로 분리.

### 2. 애드센스 정책 위반 진단 및 조치

- **"게시자 콘텐츠 없는 화면에 광고"**: `layout.tsx`에 애드센스 스크립트가 사이트 전역으로
  로드되는데, `/nexadmin`·`/dashboard/**`·`/login`·`/callback`·`/verify`·`/overlay/**`는
  로그인 없이는 빈 화면/로딩만 뜨고, `/status`는 정책이 명시한 "알림·이동 목적 화면"에
  해당함 — `robots.ts` 신설(`4a95c57`, `/status` 추가는 `ea65b57` 이전 커밋)해서 이
  경로들을 크롤링 대상에서 제외.
- **"가치가 별로 없는 콘텐츠"**: 공개 페이지(`/`, `/faq`, `/guide`) 텍스트 분량이 애드센스
  최소 기준 대비 얇다는 진단만 하고, 코드로 자동 해결되는 문제가 아니라서 콘텐츠 보강
  방향을 이후 항목(3, 4)으로 실제 착수함.
- **주의**: 애드센스 자동 광고(Auto ads)에 맡기기로 결정 — 광고 슬롯 ID(`data-ad-slot`)를
  수동으로 넣는 방식은 실제 콘솔 값이 있어야 해서 보류. `/community`처럼 크롤링이 허용된
  새 콘텐츠 페이지는 별도 코드 없이 자동으로 광고 게재 대상이 됨.

### 3. 공개 커뮤니티 홍보 페이지 신설 (`9fa102d`, `22d3386`, `e676d98`, `7e4b78e`)

원래는 서버 디렉토리+검색+실시간 급상승+통합 명예의 전당까지 포함한 큰 기획(리더보드
페이지)이 제시됐으나, 포인트 이력 테이블 부재·서버마다 다른 포인트 배율로 인한 통합 랭킹의
공정성 문제·파일 업로드 인프라 부재 등을 이유로 플랜 모드에서 범위를 계속 좁히다가, 최종
적으로 사용자가 전체 보류를 선택 → 대신 "커뮤니티(홍보) 목적의 공개 페이지 1개"로 재요청.

- DB: `community_listing`(guild_id, is_public, description, invite_url, updated_at).
- 백엔드: `community_router.py` 신설 — 관리자용 `GET/PUT /api/community/{guild_id}/settings`
  (`require_guild_admin` 재사용), 공개용 `GET /api/community/list`(인증 없음, `is_public=1`인
  서버만, 길드 목록은 로컬 120초 캐시로 Discord API 과다 호출 방지).
- 프론트: 대시보드 "일반 설정" 탭에 공개 토글+소개 문구 카드 추가, 로그인 불필요한
  `/community` 공개 페이지 신설(서버 아이콘/이름/소개글/치지직 LIVE 배지).
- **버그 1**: 토글을 클릭해도 ON이 안 됨 → `Switch`는 시각적으로 숨긴 체크박스(`sr-only`)
  구조라 `<label>`로 감싸야 클릭이 전달되는데, 다른 페이지(관리/인증)의 기존 스위치들은
  전부 `<label>`로 감싸져 있었던 반면 새로 추가한 것만 일반 `<div>`로 감싸서 발생. 수정.
- **버그 2**: 치지직 미연동 서버는 카드에 이동 버튼이 아예 없어서 방문자가 갈 곳이 없음 →
  `invite_url` 컬럼 추가, 관리자가 디스코드 초대 링크를 입력하면 "디스코드 참여하기" 버튼이
  항상 뜨도록 함(치지직 연동 시 두 버튼 다 표시, 형식 검증은 프론트에서만 `https://discord.gg/...`
  패턴 체크).
- 저장/로드 실패를 조용히 삼키던 빈 `catch{}`도 nexadmin과 동일한 패턴으로 에러 배너 추가.

### 4. `/guide` 페이지 개편 + 홈 목업 컴포넌트 공유화 (`9fa102d`)

- 기존 "5단계 설치 안내" 위주 구성을 "빠른 시작 3단계 + 기능별 활용법(텍스트+미리보기)"로
  전면 개편 — 서버관리/애정도/포인트&상점/치지직알림/팔로우인증/커뮤니티 6개 기능을 각각
  설명 문단+구체적 예시+시각적 미리보기로 소개.
- 홈페이지(`app/page.tsx`)에 인라인으로 있던 목업 컴포넌트 4개(Terminal/Points/ChzzkEmbed/
  ChzzkFollowVerify)를 `components/marketing/Mockups.tsx`로 추출해 홈/가이드가 공유하도록
  하고, Leveling/Community 목업 2개를 새로 추가.

### 5. 이용약관/개인정보처리방침 최신화 (`9fa102d`)

- 그동안 추가된 기능(포인트&상점, 치지직 실시간 채팅 명령어, 커뮤니티 홍보 페이지)을
  서비스 내용/수집 항목/이용 목적 섹션에 반영, 시행일을 2026년 7월 14일로 갱신.

### ⚠️ 다음에 이어서 확인해야 할 것

- 이번 세션 수정 전부 로컬 `py_compile`/`tsc --noEmit`만 통과했고, 실제 브라우저로
  버튼 클릭까지 확인한 건 사용자가 라이브에서 재현한 버그(스위치 클릭 안 됨, 치지직 미연동
  이동 버튼 없음) 두 건뿐 — 나머지(nexadmin 401 처리, 통계 타임아웃, robots.txt 크롤링
  차단, 가이드 페이지 레이아웃)는 아직 실사용 환경에서 눈으로 확인 안 됨.
- 애드센스 "가치가 별로 없는 콘텐츠" 재검토 요청은 아직 안 넣음 — `/community`에 실제
  등록 사례가 쌓이고 `/guide` 개편이 반영된 뒤 재심사 요청할 것.
- 서버 디렉토리/검색/실시간 급상승/통합 명예의 전당(원래 기획안의 나머지 부분)은 보류
  상태. 플랜 문서가 `~/.claude/plans/async-zooming-pumpkin.md`(로컬, 저장소 밖)에
  남아있으니 재개 시 참고 — 단, 그 플랜은 "서버별 개별 랭킹 페이지"(slug 기반) 버전이라
  최종적으로 구현된 "커뮤니티 통합 목록 페이지" 방향과 다르므로 그대로 재사용하지 말고
  아이디어만 참고할 것.
- 커뮤니티 페이지 `invite_url` 형식 검증이 프론트에만 있고 백엔드는 길이 제한 외에
  형식을 검증하지 않음 — API를 직접 호출하면 임의 URL이 저장될 수 있음(같은 서버 소유자가
  자기 데이터를 입력하는 구조라 위험도는 낮다고 판단해 의도적으로 보류).

## 2026-07-09 세션

### 1. 치지직 인증 자동 정리 (`64ec481`)
- **문제**: 서버에서 유저가 나가거나 봇이 서버에서 추방/탈퇴해도 `chzzk_verifications`
  행이 그대로 남아, nexadmin 상단 "치지직 인증 N명" 카드(`GET /api/admin/overview`가
  단순 `COUNT(*)`)가 실제 유효 인증자 수가 아니라 계속 누적되기만 하는 값처럼 보였음.
  참고로 하단 인증 목록(`/api/admin/verifications`)은 "봇이 없는 서버"만 화면에서
  걸러낼 뿐 DB에서 지우지는 않았고, "유저가 서버만 나간" 경우는 걸러내는 로직 자체가
  없었음.
- 세 단계로 정리 경로를 갖춤:
  1. `cogs/welcome.py`의 `on_member_remove`에서 goodbye 메시지 처리보다 먼저
     `DELETE FROM chzzk_verifications WHERE guild_id=? AND user_id=?` 실행 — 유저가
     서버를 나가면 즉시 그 서버의 인증 행만 삭제.
  2. `main.py`에 `on_guild_remove` 핸들러 추가 — 봇이 서버에서 나가면(추방 포함)
     해당 `guild_id`의 인증 행 전체를 즉시 삭제.
  3. `main.py`에 6시간 주기 `reconcile_verifications` 태스크 추가 — 봇이 오프라인이던
     동안 놓친 `on_member_remove` 이벤트를 보정하기 위해, `chzzk_verifications`에 남은
     모든 guild_id를 순회하며 봇이 이제 없는 서버는 통째로, 봇이 있는 서버는 실제
     `guild.members` 캐시와 대조해 더 이상 멤버가 아닌 유저의 인증 행만 골라 삭제.
- 결과: nexadmin의 인증 카운트/목록이 "누적치"가 아니라 "현재 유효 인증자 수"에
  수렴하도록 동작 방식이 바뀜.

## 2026-07-08 세션

### 1. 포인트상점 셀렉트 메뉴 전환 (`160fdfb`)
- 기존엔 `/포인트상점`을 치면 등록된 아이템 개수만큼 임베드+버튼 메시지가 채널에 줄줄이
  올라오는 스팸 형태였음.
- 드롭다운(`discord.ui.Select`) 하나로 통합: 상품명 + 필요 포인트 + 재고 + 설명을 옵션
  설명에 표시(요청에 따라 이미지는 제외). 상품을 선택하면 본인에게만 보이는 상세 임베드 +
  "교환하기" 버튼이 뜨고, 실제 차감 로직(`_handle_shop_buy`)은 그대로 재사용.
- 셀렉트도 기존 교환 버튼처럼 persistent view로 등록해 봇 재시작 후에도 예전에 올라온
  상점 메시지가 계속 동작하도록 함.
- 알려진 제약: Discord 셀렉트 메뉴는 옵션 최대 25개 — 활성 아이템이 25개를 넘으면 상위
  25개만 노출(페이지네이션 없음, 현재 규모에선 발생 가능성 낮다고 판단해 보류).

### 2. nexadmin 봇 상태 수동 새로고침 (`160fdfb`)
- **문제**: 디스코드 봇 프로필의 "N개의 서버" 상태 표시(presence)가 `on_ready`에서 딱
  한 번만 설정되고 이후로는 갱신되지 않아, 봇 프로세스가 재시작되는 배포(push) 시점에만
  새 숫자가 반영되는 것처럼 보였음. 별개로 nexadmin의 서버 개수도 Discord API 응답을
  2분(TTL) 메모리 캐싱하고 있어 새로고침 버튼을 눌러도 캐시 만료 전이면 그대로였음.
- `main.py`에 `_refresh_stats_and_presence()` 공용 헬퍼를 추출해 기존 30분 주기
  `update_stats` 루프가 이제 presence까지 함께 자동 갱신하도록 수정.
- nexadmin "새로고침" 버튼 → `POST /api/admin/refresh` 호출 → (1) 백엔드 서버 목록 캐시를
  `force=True`로 즉시 무효화, (2) `bot_stats.refresh_requested_at`에 타임스탬프를 찍어
  봇 프로세스에 신호. 봇·백엔드는 별도 프로세스라 직접 호출이 불가능해서, 이 프로젝트에
  이미 있던 "공유 SQLite를 신호 채널로 쓰는" 패턴(치지직 채팅 테스트 큐와 동일한 방식)을
  그대로 따름 — 봇은 `poll_manual_refresh` 루프로 10초마다 이 타임스탬프를 확인하고,
  바뀌어 있으면 즉시 presence/통계를 재계산.

### 3. 출석체크 미인증 무응답 → 안내 메시지 (`9904b3e`)
- **증상**: `!출석체크`가 안 된다는 신고. `!포인트`/`!도박` 등 다른 명령어는 정상 동작.
- **원인**: `_handle_checkin`(`cogs/chzzk_chat.py`)이 `chzzk_verifications`에 없는(=대시보드
  "입장 인증"으로 디스코드 계정과 연동 안 된) 치지직 유저를 로그만 남기고 완전히 무시하도록
  되어 있었음. `!포인트`는 미인증이어도 안내 메시지를 보내서 "다른 건 되는데 얘만 안 되는"
  것처럼 보였던 것 — 실제로는 예외 처리 문제가 아니라 의도된 조용한 게이트였음.
- 다른 내장 명령어와 동일하게 안내 메시지를 보내도록 수정(문구는 "디스코드 서버 인증이
  필요합니다!"로 확정).

### 4. nexadmin 인증 현황 닉네임이 유저 ID로 표시되는 문제 (`9904b3e`)
- **원인**: `/api/admin/verifications`, `/api/admin/follow-stats`가 각 유저의 디스코드
  닉네임을 `asyncio.gather`로 전부 동시에 개별 조회(`GET /guilds/{id}/members/{user_id}`)하는데,
  등록 인원이 많은 길드(예: 생동감의 방공호)일수록 그 길드의 member-fetch 레이트리밋(429)에
  걸려 실패 시 조용히 `user_id`로 폴백하고 있었음. 인원이 적은 길드는 안 걸려서 정상 표시.
- `_fetch_member_name`(`admin_router.py`)에 동시 요청 제한(세마포어 5개) + 429 시
  `retry_after`만큼 대기 후 1회 재시도 추가.

### 5. 치지직 스트리머 계정 중복 서버 등록 방지 (`e4f27d0`)
- **문제**: 같은 치지직 계정으로 서버 A에 스트리머 연동을 해둔 상태에서 서버 B에도 다시
  스트리머 연동을 하면 각각 DB에는 등록되지만, `cogs/chzzk_chat.py`의 `self._channels`가
  `chzzk_channel_id` 하나로만 키가 잡혀 있어 두 서버 모두 채팅 구독을 켜면 나중에 동기화된
  서버가 이전 서버의 채널 항목을 덮어써 메시지가 엉뚱한 서버로 처리되는 오류가 있었음.
- `chzzk_auth_router.py`의 `_handle_streamer_registration`에 등록 전 중복 검사 추가 —
  다른 guild_id에 이미 등록된 chzzk_channel_id면 OAuth 완료를 거부하고 `_get_guild_name`
  으로 조회한 상대 서버 이름과 함께 `?error=chzzk_already_registered&other_guild=` 로
  리다이렉트. `web/frontend/app/dashboard/[guildId]/chzzk/page.tsx`는 이 에러 쿼리파라미터를
  읽어 배너로 표시(이 페이지는 이전까지 success 파라미터만 처리하고 error는 아예 무시했음).

### ⚠️ 다음에 이어서 확인해야 할 것
- 두 기능 다 배포만 하고 실제 디스코드/브라우저로 눈으로 확인은 아직 안 함. `/포인트상점`
  셀렉트 메뉴 UX, nexadmin 새로고침 클릭 후 실제 봇 프로필 상태 텍스트가 10초 내 바뀌는지
  확인 필요.
- `bot_stats`에 기존 행이 없는 초기 상태에서 `/api/admin/refresh`를 먼저 호출하면
  `guilds`/`chzzk_subs`가 0으로 잠깐 insert됐다가 봇의 다음 폴링(최대 10초)이나 30분
  루프에서 실제 값으로 덮어써짐 — 그 짧은 사이 nexadmin 통계 카드가 순간적으로 0을 보여줄
  가능성 있음(실사용 환경엔 이미 행이 존재해 해당 없음).
- 출석체크/nexadmin 닉네임/스트리머 중복 등록 세 건 모두 코드 리뷰 기반 수정이라, 실제
  치지직 채팅 및 nexadmin 화면으로 눈으로 확인은 아직 안 함.

## 2026-07-05 ~ 07-06 세션

### 1. 대시보드 잡음 정리 + 성능 버그 (`0e393a8`)
- 일반설정 "애정도 레벨업 알림 채널"의 `현재 채널 (기본)` 플레이스홀더를 다른 채널 선택
  필드와 동일한 `채널 선택...`으로 통일 (실제 폴백 동작 자체는 그대로 유지, 문구만 정리).
- 입장 인증 임베드 미리보기: 버튼 색상이 임베드 색상을 따라가도록 되어 있었는데, 실제
  Discord 버튼은 `ButtonStyle.success`(초록)/`ButtonStyle.link`(회색) 고정이라 미리보기가
  거짓 정보를 보여주고 있었음 → 실제 스타일대로 고정, 안내 문구 추가.
- 치지직 탭: 스트리머 팔로워 조회 연동 안내/버튼을 "팔로워 역할 지급" 카드 안으로 이동
  (구분선으로 분리), "사용방법" 토글 버튼 추가.
- **성능 버그**: `require_guild_admin`이 Discord `/users/@me/guilds`를 호출해 5분 캐싱하는데,
  캐시가 빈 상태에서 대시보드 한 페이지가 여러 API를 동시에 호출하면 각 요청이 독립적으로
  중복 호출하고 있었음 (새로고침/탭 전환 시 느려지던 원인) → 같은 유저·서버에 대한 동시
  요청을 in-flight 태스크 하나로 합치도록 수정.

### 2. Discord 로그인 "잘못된 redirect_uri" 간헐적 오류 (`7840375`)
`/login` 페이지가 `NEXT_PUBLIC_DISCORD_REDIRECT_URI`(프론트 빌드 시점 고정값)로 직접 인증
URL을 만들고, 홈페이지/verify 페이지의 로그인 버튼은 백엔드 `/api/auth/login`
(`DISCORD_REDIRECT_URI`, 서버 런타임 값)을 호출 — 두 값이 어긋나 있으면 **어느 진입점으로
로그인을 시작했는지**에 따라 되기도 하고 안 되기도 했음 (세션 만료로 `/login`에 자동
리다이렉트되는 경우가 특히 잘 걸림). `/login`도 항상 백엔드 값을 쓰도록 통일, 안 쓰는
프론트엔드 환경변수/문서 정리.

### 3. 마크 콜라보 이벤트 — 이번 세션의 핵심 신규 기능 (`b178319` ~ `73a5fd2`)
**요구사항**: 여러 스트리머가 함께하는 마인크래프트 콜라보에서, 각 스트리머 방송 채팅에
`!디버프지급`/`!버프지급`/`!랜덤아이템`을 치면 기존 포인트를 차감하고 공유 마크 서버에
RCON으로 효과를 실행. 디버프는 참가자 중 무작위 1명(자기 제외)에게, 버프는 자기 자신에게.

**아키텍처**: 새 웹사이트를 따로 만들지 않고 기존 nexbot.shop 안에서 해결.
- 설정/카탈로그 관리는 **nexadmin(오너 전용)** — 여러 길드를 가로지르는 크로스길드 개념이라
  개별 스트리머 대시보드에 두기엔 부적합 (RCON 비밀번호 노출, 서버마다 다른 카탈로그로
  밸런스 깨짐 등).
- 실제 구매/발동은 **치지직 실시간 채팅**(`cogs/chzzk_chat.py` 기존 인프라 재사용) — 기존
  `!출석체크`와 동일하게 `chzzk_verifications`로 유저를 매핑하고 `user_points`에서 차감.
- 마크 서버 연동은 **RCON** 직접 구현(`utils/mc_rcon.py`, 외부 의존성 없음) — 봇 프로세스가
  마크 서버로 단방향 명령만 보내면 되므로 플러그인 설치 불필요. RCON 포트는 반드시 봇 서버
  IP만 방화벽 허용해야 함(외부 공개 금지).

**DB**: `mc_events`(이벤트+마크 접속정보, 한 번에 하나만 활성화), `mc_event_guilds`
(참가 서버+마크 플레이어 이름), `mc_event_items`(카탈로그: 효과 명령어+대상 알림 명령어+
채팅 안내 문구, `{player}`/`{user}`/`{item}` 플레이스홀더), `mc_event_commands`(트리거
문구, 이벤트당 자유롭게 추가/이름변경/비활성화 가능 — 이벤트 생성 시 3개 기본값 자동 등록),
`mc_event_purchases`(구매/적용 로그).

**참가 흐름 (B안으로 확정)**: nexadmin에서 오너가 길드를 "초대"(마크 닉네임은 몰라도 됨) →
초대된 길드의 자기 대시보드에 "이벤트 서버 (합방)" 탭이 새로 나타남(초대 안 된 길드에는
안 보임) → 그 서버 관리자가 직접 마크 닉네임 입력, 치지직 연동 상태 확인, 등록된 명령어/
카탈로그 열람(읽기 전용).

**실전에서 잡은 버그들**:
- 참가 서버 삭제가 안 되던 문제 — Discord 스노우플레이크 ID가 JS `number`의 안전 정수
  범위(2^53)를 넘어서 JSON 응답에 숫자로 내려주면 브라우저에서 반올림되어 삭제 요청이 존재
  하지 않는 ID로 나감 → 응답을 문자열로 변경 (이 프로젝트에서 이미 `admin_router.py`가
  같은 이유로 문자열 캐스팅하고 있던 걸 이번 라우터에 놓쳤던 것).
- `chzzk_chat.py` 기존 로직에 잠재 버그: `chzzk_chat_commands`가 하나도 없는 길드는 채팅
  구독 자체를 안 하던 것 → 마크 이벤트만 참가하고 출첵/자동응답은 설정 안 한 길드는 채팅을
  영영 못 받는 문제였음 → mc_event 존재 여부도 함께 확인하도록 수정.
- "미인증 유저"/"아이템 없음"/"트리거 매칭 실패" 케이스가 대부분 조용히 무시되기만 하고
  로그가 안 남아 디버깅이 안 됐음 → 전부 `log.info`로 남기고, 매칭 실패 로그엔 `mc_event`
  자체가 없는 건지(참가 미등록) 트리거만 안 맞는 건지 구분되게 문구 개선.
- 채팅 명령어 이름을 바꾸는 UI가 애초에 없었음(추가/삭제만 가능) → 인라인 편집 추가.
- nexadmin "초대" 판정이 이벤트 `is_active=1`을 요구해서, 초대만 하고 아직 활성화 전이면
  참가자 대시보드에 탭 자체가 안 보이던 문제 → 초대 여부와 활성화 여부를 분리 (활성화는
  실제 채팅 명령어 동작 여부에만 관여).
- `chzzk_chat.py`의 설정 동기화 주기가 5분이라, 스트리머 연동 직후나 명령어/이벤트 설정을
  바꾼 직후 반영까지 최대 5분 걸려 "방금 한 설정이 안 먹힌다"처럼 보이는 문제가 반복 발생
  → 60초로 단축.

### ⚠️ 다음에 이어서 확인해야 할 것
- **실제 마크 서버 붙여서 RCON 연동 테스트는 아직 안 함.** nexadmin의 "연결 테스트"/
  "테스트 명령어"/아이템별 "테스트 실행" 버튼으로 서버 열리면 확인 예정.
- 참가자가 1명뿐일 때 디버프/랜덤아이템은 "자기 자신에게 적용"으로 대체하도록 만들어둠(원래
  설계는 자기 제외 무작위 1명) — 실제 10명이 다 채워지면 이 폴백은 안 타고 정상적으로
  무작위 상대에게 적용됨.
- 참가 길드가 여러 이벤트에 걸쳐 초대된 적이 있으면 `get_mc_event_status`가 **가장 최근
  이벤트 하나만** 기준으로 삼음 — 오래된 이벤트 참가 기록을 nexadmin에서 정리하지 않으면
  계속 그 길드 기준으로 잡힐 수 있음.

## 2026-07-04 ~ 07-05 세션

### 1. 보안 QA 및 수정 (`0604eb8`, `3ec5e6c` 이전 커밋들과 별개 세션)
- **`guilds_router.py` IDOR**: `/channels`, `/roles`, `/members/search`가 관리자 권한 체크
  없이 아무 로그인 유저나 임의의 `guild_id`를 조회할 수 있던 문제 → `require_guild_admin` 추가.
- **`chzzk_auth_router.py` 인증 우회**: `/login`, `/streamer-login`이 `discord_user_id`를
  클라이언트 입력값으로 그대로 신뢰하던 문제(가짜 유저에게 역할 부여 가능, 스트리머 연동
  탈취 가능) → JWT 세션에서 직접 유저 ID를 추출하는 `/login-url`, `/streamer-login-url`로 교체.
- **JWT-in-URL**: 로그인 완료 후 토큰을 쿼리스트링(`?token=`)으로 전달하던 것을 URL
  프래그먼트(`#token=`)로 변경 — 서버 로그/Referer에 안 남도록.
- **상점 구매 TOCTOU 레이스**: 잔액 확인과 차감이 분리되어 있어 연타 시 초과 구매 가능하던
  것을 단일 원자적 `UPDATE ... WHERE points >= cost`로 수정.

### 2. 대시보드 디자인 시스템 정리 (`3ec5e6c` ~ `69ae00c`)
- `globals.css` 타이포 스케일/색상 토큰 재정의 (제목 크게, 카드 여백 넉넉하게).
- 포인트/nexadmin/레벨업/관리/치지직 페이지에 상단 요약 통계 타일 추가.
- nexadmin "인증 현황" 탭을 서버별로 그룹핑.
- 사이드바를 **서버 관리 / 방송 / 지원** 3개 카테고리로 분리 (구분선 + 라벨).
- 페이지마다 제각각이던 하드코딩 색상(green-400, red-400 등)을 디자인 토큰(`success`/
  `danger`/`warning`/`accent`/`chzzk`)으로 통일.

### 3. 사이트 공지 배너 (`72e0c34`)
- nexadmin에 "공지 관리" 탭 추가 → 저장하면 랜딩 페이지 상단에 `공지: 내용` 배너로 표시,
  X로 닫으면 그 문구를 localStorage에 기억해뒀다가 **다른 공지로 바뀌면 다시 노출**.
- `site_announcement` 테이블(단일 행), `GET/PUT /api/admin/announcement`(오너 전용),
  `GET /api/stats/announcement`(공개).

### 4. 레벨업 → 애정도 리네이밍 (`1986868`, `2dc017c`)
- 봇 임베드(`/랭크`, `/리더보드`, `/xp설정`, 레벨업 알림), 대시보드(사이드바, 레벨업 페이지,
  명령어 목록, 일반설정, 가이드, 랜딩 페이지) 전반의 표시 텍스트를 "애정도"로 변경.
- **DB 컬럼명(`user_xp`, `level_rewards` 등)은 그대로** — 순수 텍스트/아이콘 변경만.
- 아이콘: 노란 번개(Zap) → 핑크 하트(Heart, `#EB459E`).

### 5. 방송설정 → 치지직/유튜브/숲 분리 (`1986868`)
- 기존에 하나의 사이드바 항목("방송설정") 안에서 플랫폼 탭으로 전환하던 구조를,
  사이드바 최상위 항목 3개(치지직/유튜브/숲)로 분리. 유튜브/숲은 준비중 페이지.
- 치지직 페이지 자체에는 "스트리머 설정" / "실시간 채팅 명령어" 세부 탭 신설.

### 6. 치지직 실시간 채팅 명령어 — 이번 세션의 핵심 신규 기능 (`467d01c` ~ `69da220`)
**요구사항**: 치지직 생방송 채팅에서 `!출석체크`(커스터마이즈 가능)를 치면 하루 1회
포인트+애정도를 지급하고, 그 외 최대 5개까지 등록 가능한 커스텀 명령어는 지정한 문구로
자동 응답.

**아키텍처 결정**: 비공식 쿠키 스크래핑 대신 치지직 **공식 오픈 API의 세션(Session)
기능**을 사용 — 이미 스트리머 연동 시 저장해둔 OAuth 토큰을 재사용. 프로토콜(Engine.IO v3
기반, 표준 `python-socketio`로는 안 됨)은 직접 구현하지 않고 이미 이걸 구현해 둔
[`chzzkpy`](https://pypi.org/project/chzzkpy/) 라이브러리를 실제로 설치해 API 시그니처를
검증한 뒤 사용.

**구현물**:
- DB: `chzzk_chat_commands`(guild당 checkin 1개 + reply 최대 5개), `chzzk_checkin_log`
  (1일1회 강제, PK 충돌로 중복 차단), `chzzk_chat_log`(디버그용 최근 50개 채팅 로그),
  `chzzk_subscriptions.chat_last_sync_at`/`chat_last_event_at`(연결 상태 표시용).
- 스트리머 OAuth 스코프에 "채팅 메시지 조회"/"채팅 메시지 쓰기" 추가 — **기존에 연동된
  스트리머는 재연동 필요**.
- `cogs/chzzk_chat.py`(신규): 세션 연결 → 스트리머별 구독 → 채팅 이벤트 수신 → 명령어
  매칭 → 지급/응답. 5분마다 동기화 + 게이트웨이 생존 확인(끊겼으면 강제 재연결).
- 대시보드 "실시간 채팅 명령어" 탭: 명령어 CRUD, 연결 상태 카드, 실시간 채팅 미리보기
  (기본 숨김, 버튼으로 열람 — 실제 수신 메시지/봇 응답을 그대로 보여주는 디버그 도구).
- 입장 인증 탭에 임베드 미리보기 토글 버튼 추가.

**실전에서 잡은 버그들** (라이브 로그 보고 그 자리에서 수정):
- `Client.connect()`의 반환값이 구독 API가 요구하는 세션 키가 아니라 소켓 transport
  ID였음 → `on_connect` 이벤트로 진짜 세션 키를 받도록 수정 (`NOT_FOUND` 에러 해결).
- 위 수정 후에도 "연결됨" 표시와 무관하게 웹소켓이 조용히 죽을 수 있는 구조적 문제 발견
  → 동기화 주기마다 `Client._gateway[sid].is_connected`를 직접 확인해 재연결.
- nexadmin 페이지가 React Hooks 규칙 위반(`useMemo`가 조건부 return 뒤에 있었음)으로
  크래시 → 프로덕션에서 "This page couldn't load"로 나타났던 원인.
- 채팅 미리보기의 `scrollIntoView`가 페이지 전체 스크롤까지 건드려 화면이 튀던 문제
  → 내부 컨테이너의 `scrollTop`만 조정하도록 수정.

### ⚠️ 다음에 이어서 확인해야 할 것
- **출석체크가 실제로 지급까지 되는지 아직 end-to-end로 확인 안 됨.** 채팅 수신 자체는
  확인됐지만(미리보기에 `!출석체크`가 뜸), 지급 로그(`치지직 출석체크 지급: ...`)가 찍히는지,
  실제 포인트/애정도가 오르는지, 치지직 채팅에 "OOO님이 N번째 출석 하셨습니다!" 안내가
  뜨는지는 확인 대기 중.
- 출석체크는 **시청자 인증(`chzzk_verifications`)** 을 마친 계정만 대상 — 스트리머 본인이
  테스트하려면 스트리머 OAuth 연동과 별개로 그 서버의 "입장 인증" 페이지에서 본인 계정으로
  시청자 인증을 한 번 완료해야 함 (자세한 건 이 파일의 CLAUDE.md 아키텍처 섹션 참고).
- `chzzkpy`는 이 프로젝트에서 실사용 검증이 이번이 처음이라, 위에서 잡은 것 외에 또 다른
  라이브러리 내부 이슈가 나올 가능성이 있음. `cogs/chzzk_chat.py`의 로그(`log.info`/
  `log.exception`)를 우선 확인.
- `chzzkpy`가 `aiohttp`/`pydantic` 버전을 정확히 핀 고정해서 설치하므로(`requirements.txt`
  참고), 배포 환경에서 다른 패키지와 버전 충돌이 나면 그쪽을 의심할 것.
